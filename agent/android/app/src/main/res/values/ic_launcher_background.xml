<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Warm White from the muster palette. It matches the plate the mark sits
         on in the source artwork, so the adaptive mask never exposes a seam
         between the foreground layer and what is behind it. -->
    <color name="ic_launcher_background">#FAF7F2</color>
</resources>
